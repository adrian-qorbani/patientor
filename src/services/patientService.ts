import patientsData from "../../data/patient_entries.json";

const getEntries = () => {
  return patientsData;
};

const addEntry = () => {
  return null;
};

export default {
  getEntries,
  addEntry,
};
